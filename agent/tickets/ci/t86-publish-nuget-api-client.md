---
id: T86
title: Content-hash versioning and automated publishing for UPM and NuGet packages
status: design-needed
depends_on: [T71]
---

# T86: Content-hash versioning and automated publishing for UPM and NuGet packages

## Goal

Automate version bumping and publishing for all 4 packages in `publish-upm.yml`, with content-hash-triggered semver and version pinning back into consumer manifests. Currently versions are bumped manually — if someone changes package source but forgets to bump the version, CI silently skips publishing and the registry stays stale. This already bit us with the NuGet API client (`org.nuget.placeframeapiclient@0.1.3` missing `LocalizationMetrics` fields).

Packages in scope:
- `org.outernet.placeframe` (Core) — npmjs.org
- `org.outernet.placeframe.arfoundation` — npmjs.org
- `org.outernet.placeframe.magicleap` — npmjs.org
- `PlaceframeApiClient` (→ `org.nuget.placeframeapiclient` via UnityNuGet) — NuGet.org

Out of scope: `org.outernet.cesium-unity` — different lifecycle (manual `workflow_dispatch`, upstream version tracking, 30-60 min build). Can reuse the manifest-update mechanism later.

## Context

The 3 UPM packages are published by `publish-upm.yml` to npmjs.org. Each step checks `npm view` and skips if the version already exists — but the version must be bumped manually in each `package.json`. Same failure mode as NuGet: human forgets to bump → CI skips → stale.

The NuGet API client is generated from the OpenAPI spec via `uv run generate-clients`. Source lives at `packages/generated/csharp/api-client/src/PlaceframeApiClient/`. The publish pipeline is: `dotnet pack` → `.nupkg` → NuGet.org → UnityNuGet service auto-converts to npm → Unity consumers resolve from `unitynuget-registry.openupm.com`. NuGet.org currently has versions up to `0.1.4` (manual publishes). The `.csproj` is regenerated on every `generate-clients` run with version `0.1.0` (hardcoded in `scripts/openapi-generator/configs/csharp.json`), so the version must be overridden at pack time via `dotnet pack -p:Version=X.Y.Z`.

Consumer projects (MapRegistrationTool, Outernet.Client) pin exact versions in their `manifest.json`. Unity's manifest.json only supports exact semver — no ranges, no `latest`, no wildcards, no variable interpolation. This means published versions must be committed back into consumer manifests. This is unavoidable duplication (unlike `.env.lock` where compose files read `${VARIABLE}` from the lock — Unity has no equivalent).

The version-pinning-back-into-consumers problem follows the same pattern as `.env.lock` for Docker digests: CI produces a value that can't be known until build time, then commits it back so the repo is self-consistent at every commit.

### NuGet pipeline details

- The `.csproj` targets `netstandard2.1` only
- `.csproj` is regenerated by openapi-generator — cannot add MinVer/Nerdbank.GitVersioning to it, cannot persist version in it
- `dotnet pack -p:Version=X.Y.Z` overrides the version at pack time without touching generated files
- No NuGet auth configured in CI yet — needs `NUGET_API_KEY` GitHub secret (manual setup)
- UnityNuGet auto-picks up new versions from NuGet.org (~10 min polling) — no PR needed to their registry after initial whitelist
- Non-Unity .NET consumers can use the package directly from NuGet.org

### API client dual identity

The API client has two package names:
- `com.placeframe.api-client` — local dev (file reference in `packages/unity/Placeframe/Packages/manifest.json`)
- `org.nuget.placeframeapiclient` — published (auto-generated by UnityNuGet from NuGet package ID)

The placeframe Core package uses the API client (`using PlaceframeApiClient.Model;` etc.) but doesn't declare it as a dependency in its published `package.json`. Each consumer adds `org.nuget.placeframeapiclient` independently. This is arguably a packaging gap — the dependency should be declared by the package that uses it.

## Key files

- `.github/workflows/publish-upm.yml` — existing UPM publish workflow (adding NuGet steps + content-hash logic)
- `packages/unity/Placeframe/Assets/Package/Core/package.json` — UPM package version
- `packages/unity/Placeframe/Assets/Package/ARFoundation/package.json` — UPM package version
- `packages/unity/Placeframe/Assets/Package/MagicLeap/package.json` — UPM package version
- `packages/generated/csharp/api-client/src/PlaceframeApiClient/PlaceframeApiClient.csproj` — .NET project file (regenerated, version overridden at pack time)
- `scripts/openapi-generator/configs/csharp.json` — generator config (hardcodes `"packageVersion": "0.1.0"`)
- `apps/MapRegistrationTool/Packages/manifest.json` — consumer manifest
- `legacy/Outernet.Client/Packages/manifest.json` — consumer manifest

## Open questions

- **Lock file format and location**: Where does the version + content hash state live? Needs to survive `generate-clients` runs (which wipe `packages/generated/csharp/api-client/`). Candidate: `.github/publish-state/` or similar.
- **Should placeframe Core declare `org.nuget.placeframeapiclient` as a dependency?** This would make it a single source of truth for that version (consumers get it transitively). But the dual identity (`com.placeframe.api-client` locally vs `org.nuget.placeframeapiclient` published) creates a conflict during development. Can Unity handle both names pointing to the same assembly?
- **What constitutes "package source" for hashing?** For NuGet: all `.cs` files in the generated source. For UPM: `.cs` files, `.asmdef`, resources, shaders — but excluding `package.json` version field (which is what gets bumped).
- **CI loop prevention mechanism**: `paths-ignore` for the state/lock files (like `.env.lock`), or `[skip ci]` in commit messages? Both are proven patterns.
- **NuGet API key setup**: Need `NUGET_API_KEY` as a GitHub Actions secret. Manual step — user creates key on NuGet.org.

## Done when

- [ ] CI content-hashes package source and auto-bumps patch version when content changes
- [ ] CI publishes 3 UPM packages to npmjs.org and NuGet API client to NuGet.org
- [ ] No publish occurs when content is unchanged
- [ ] Published versions committed back into consumer manifests (lock file pattern)
- [ ] Published NuGet package includes all current `LocalizationMetrics` fields
- [ ] Unity projects compile without `LocalizationMetrics` errors (after UnityNuGet picks up new version)
- [ ] Version state (version + hash) persisted in repo for archaeological traceability
- [ ] Strategy documented

## Design decisions

- **Publish NuGet to NuGet.org, not npmjs.org directly**: NuGet.org is the standard .NET registry. Non-Unity .NET consumers can use the package directly. UnityNuGet auto-converts for Unity consumers.
- **All 4 packages in `publish-upm.yml`**: Keeps all Unity-related package publishing in one place. The workflow is thin and fast. (Previously T87 was a separate ticket for the UPM packages — folded in since it's the same problem.)
- **Content-hash-triggered semver**: Hash source to detect changes, auto-bump patch, publish only when content differs. Clean semver versions, no noise publishes.
- **Lock file commit-back (like `.env.lock`)**: CI produces the version, commits state back to repo. The repo is the source of truth — `git log` at any commit shows exactly which package versions that code ran against.
- **Consumer manifest duplication is unavoidable**: Unity manifest.json doesn't support variable interpolation, ranges, or `latest`. Exact semver strings must be in the JSON. Unlike `.env.lock` where compose files read `${VARIABLE}`, there's no indirection mechanism. `resolutionStrategy` only applies to indirect dependencies.
- **Cesium-unity excluded**: Different lifecycle (manual trigger, upstream version, 30-60 min build). Can reuse the manifest-update mechanism later.

## Next step

Resolve open questions (lock file format, Core dependency declaration, hash scope, loop prevention). Then create plan.
